# 仓库指南

## 项目结构与模块组织

本仓库的主项目是基于 Zynq-7020B 的 FPGA 机器视觉 Portal 智能解谜平台。根目录下的 `*.md` 文件包含项目方案、选题评估和获奖作品调研；其中 `FPGA机器视觉Portal智能解谜平台_新版方案.md` 是当前项目方案，其余文档作为设计依据和背景资料。相关图片应放在引用文档附近，并使用相对路径链接。`image-20260913232229096.png` 是文档共用素材。

`portal-showcase/` 是智能解谜平台的浏览器交互原型：`index.html` 定义页面结构，`styles.css` 负责视觉样式与响应式布局，`app.js` 实现交互和光线追踪逻辑。`color_test/` 包含 AX7Z020B 的 Vivado 工程、HDL、约束、OV5640 裸机软件、构建脚本和测试；生成的工作区与二进制产物不提交。

## 构建、测试与本地开发

项目无需安装依赖或编译。在仓库根目录运行：

```powershell
python -m http.server 8000 --directory portal-showcase
```

随后访问 `http://localhost:8000` 检查网页原型。快速检查时也可直接打开 `portal-showcase/index.html`。提交前执行 `git diff --check` 检查空白符问题，并使用 `git status --short` 确认只包含预期文件。

## 编码风格与命名约定

中文文档和界面文本统一使用 UTF-8 编码。Markdown 应采用清晰的标题、简短段落、带语言标记的代码块和相对资源路径。HTML、CSS 与 JavaScript 沿用现有的两空格缩进和分号结尾风格。

JavaScript 变量及函数使用 `camelCase`，常量使用 `UPPER_SNAKE_CASE`，CSS 类名和元素 ID 使用 kebab-case。除非需求明确要求，否则保持网页原型零依赖。

涉及硬件设计时，文档、约束和工程配置必须统一注明目标板卡为 `Zynq-7020B`。不要在未验证的情况下填写具体芯片封装、速度等级、引脚或时钟参数；这些信息应以板卡原理图和厂商资料为准。

## 测试要求

仓库目前没有正式测试框架或覆盖率指标。修改网页后，应手动验证两种场地预设、光路演示与尺寸规划模式、拖动操作、键盘控制、场景重置和窄屏布局，并确认浏览器控制台无报错。修改文档后，应预览 Markdown，确认标题、表格和图片链接均能正确显示。后续加入硬件工程时，提交说明中应记录 Vivado 版本、目标器件、综合与实现结果，以及板上验证步骤。

## 提交与拉取请求规范

当前 Git 历史仅包含初始化提交，尚未形成固定规范。提交标题应简短、使用祈使语气，例如 `docs: 完善 Portal 硬件方案` 或 `ui: 修复镜面拖动边界`。每个提交只处理一个主题。

拉取请求应说明改动内容、受影响的文档或原型行为、已执行的验证，并关联相关议题。涉及可见界面变化时，应附上修改前后的截图。=

## 硬件设备

开发板：` zynq7020B黑金板`

摄像头：` ov5640MIPI`
