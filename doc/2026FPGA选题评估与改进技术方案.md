# 2026 FPGA 选题评估与改进技术方案

更新：2026-09-14。适用团队：3 人，主要具备 Verilog 开发基础。单个候选方案的板卡及基础外设预算为 1000—2500 元。本文是选题与架构建议，性能数值除注明赛事要求外均为待验证的设计目标，不代表已实现或获奖保证。

## 1. 结论与依据边界

**当前三项候选分别匹配不同平台：Portal 优先选择易灵思 VF-Ti60F225 并对应 2026 实时图像边缘检测题；实体红石条件选择 AMD PYNQ-Z2，须先核实 2026 AMD 指南是否允许该板；声音题选择高云 Tang Nano 20K。三个方案最终只选择一个参赛，不同时购买三套完整系统。**

这不是按“哪个名字最有创意”排序，而是按当前约 7 周开发时间、3 人协作、可验证的 FPGA 工作量、现场稳定性和企业赛题匹配度作出的条件式选择。每周投入时间以及团队的摄像头、音频和 SoC 实战经验仍需在开工前确认。

已阅读本文件夹的四份 Markdown：备选题、获奖作品调研、Portal 新版方案、实体红石方案，并查看红石效果图。引用 ChatGPT 对话只提供了“生成场景图／用中文／红石也生成一张”的预览，当前工具没有可调用的完整对话读取接口，因此未声称掌握完整对话。下面对原方案的判断以本地文档为据。

本届时间已由[北京理工大学教务部 2026 年通知](https://jwb.bit.edu.cn/xs/pyfa_2/tzgg__26/97dc9f2b043740d192dd7e5454b5288d.htm)及[高云 2026 赛事页](https://www.gowinsemi.com.cn/university/match2026)交叉核实：报名截止 9 月 22 日 24 点，提交截止 11 月 4 日 18 点，决赛安排为 11 月 20—22 日；高校通知明确每队不超过 3 人。高云页面当前另列板卡申请截止 9 月 16 日。后续变动应以赛事更新为准。

赛事总通知列出的协办平台包括 AMD、安路、易灵思、高云、紫光同创、复旦微和中科亿海微，并明确参赛队伍应采用组委会指定 FPGA 开发平台；具体平台限制仍以各企业 2026 选题指南为准。当前已核实高云声音题和易灵思实时图像边缘检测题的公开要求，但尚未取得可确认 PYNQ-Z2 资格的 2026 AMD 指南。因此：

- 本文采用“2026 FPGA 创新设计赛道”称呼，不把其他赛事的届数、奖项和规则混用。
- 已核实高云第二题允许的交互形式、合成路线及 Tang Nano 20K 适配资格。
- 已核实易灵思 2026 第四题指定 VF-Ti60F225，并提供摄像头、DDR3、HDMI/LVDS 基础视频链路；Portal 必须真实完成该题要求的边缘检测，不能只借用展示外壳。
- PYNQ-Z2 目前只作为红石方案的技术首选。未核实 2026 AMD 指南前，不购买、不报名，也不声称其具备参赛资格。
- “开放式报名”不等于“允许任意自由命题”。不能因为作品用了 FPGA 就认定符合某道赛题。
- 本文只形成板卡与外设清单，不执行采购，不安装任何库。

### 1.1 当前板卡决策

| 候选方案 | 首选平台 | 参赛对应 | 采购门槛 |
|---|---|---|---|
| Portal 传送门 | 易灵思 VF-Ti60F225 | 2026 第四题：基于 FPGA 的实时图像边缘检测系统 | 先取得可运行的摄像头到 HDMI 基础工程，并确认摄像头具体型号 |
| 实体红石电路 | AMD PYNQ-Z2 | AMD 开放式自主设计方向，资格待核实 | 先由 2026 AMD 指南确认 PYNQ-Z2 或 XC7Z020 在允许范围内 |
| 实时声音合成 | 高云 Tang Nano 20K | 2026 第二题：基于 FPGA 的实时多音色合成电子乐器引擎 | 确认板载 MAX98357A 输出例程和扬声器连接方式 |

若 PYNQ-Z2 不在 2026 AMD 允许平台内，不用相近板卡名称自行替代；转而核对安路 2026 是否保留自主命题，并按其当届指定板卡重新评估红石方案。

## 2. 去年获奖方向能提供什么启示

以下是核实到的代表性样本，不是 2025 国一作品的完整统计。

| 2025 作品或方向 | 证据等级 | 对本次设计的实际启示 |
|---|---|---|
| 武汉大学“基于 FPGA 的鸟类声音识别系统” | 厂商官方明确一等奖、最佳创意奖 | 感知对象明确；可采用 CPU 与 FPGA 分工，不必所有算法都写 RTL |
| 南京大学“多模态智能人机交互系统” | 厂商官方明确一等奖、安路科技杯 | 让 FPGA 承担实时控制，CPU 承担感知决策；交互应有执行反馈 |
| 广西大学“多模态智能四探针半导体测试仪” | 厂商官方明确一等奖、安路科技杯 | 仪器化作品能够提供可测量输出，工程价值比功能数量更重要 |

上述三项来自[安路科技 2025 官方回顾](https://www.anlogic.com/news/company-news/188.html)。我据此推断值得借鉴的是“清楚的任务分工、可测指标和完整系统”，不能据此推断某种题目必然获奖。

另有两项与当前选题直接相关：

- [江南大学官方报道](https://jyxq.jiangnan.edu.cn/info/1020/1283.htm)明确：多模态成像融合方向获一等奖；基于 RISC-V 与 DSCNN 的菌类识别低功耗加速器获二等奖。它不能证明该加速器与“霉斑分割”是同一任务，但说明“菌类 + FPGA + 轻量网络”已经不是空白方向。
- [2025 安路开源工程](https://github.com/maojinxiang/FPGA-ANLU-National-First-Prize)由作者声明获国家一等奖，公开了摄像头、SDRAM、UDP、HDMI 等完整链路。适合参考接口与工程组织；本次未独立对照官方名单核实作者奖项，也未验证代码可综合或许可适用范围。

原调研文档里的 2019—2024 项目可以作技术背景；集创赛二等奖、校赛一等奖不可写成“去年本赛事国一”。[南京大学 2025 项目报道](https://ic.nju.edu.cn/39/18/c63085a801048/page.htm)介绍了内窥成像与畸变校正等项目，但不宜将未逐项明确的奖级自行补成国一。

## 3. 原方案最需要修正的部分

| 原内容 | 问题 | 建议修改 |
|---|---|---|
| 三个方向都给予很高星级评价 | 没有反映数据、硬件、时间差异 | 改为资源条件、关键风险、退出门槛 |
| 霉菌检测扩展至食品、衣物、木材、墙面 | 每增加一种材料都增加负样本、成像与泛化验证成本 | 第一版只选择一种材料、一种采集环境 |
| 检测框面积直接表示霉变程度 | 框包含大量背景，且视觉覆盖率不等于生物学风险 | 做掩膜面积；只报告可见疑似霉斑覆盖率 |
| 红石模块 ID 即拓扑 | 知道模块类型不能说明哪个输出接哪个输入 | 定义网格、朝向、端口与连接规则，或实施电气连通扫描 |
| 红石效果图用视觉，正文用扫描底板 | 两条输入链路没有主次 | 首版选固定网格扫描，视觉是有余力的升级 |
| 10—20 节点低速 Tick 必须用 FPGA | MCU 很可能也能轻松完成 | 用节点扩展、并行状态更新和硬件波形记录证明价值 |
| Portal 预测光线与真实激光混用 | 真实光束不会自行从传送门另一端出现 | 首版明确为实体道具控制数字光路，命中驱动实体机关 |
| 15 个周期算法延迟代表整机响应 | 采集、等待帧结束、缓存、显示与执行均有额外延迟 | 分别测核心延迟与端到端延迟 |

## 4. 新推荐：积木式交互实时电子乐器

### 4.1 定位与赛题匹配

建议名称：**基于 FPGA 的积木式多维交互实时合成乐器**。

保留红石方案的实体搭建、发光反馈和模块化外观，把底层运算对象改为声音。与数字电路教学版是两条备选路线，不在同一版本中同时做完整红石与完整乐器。

高云 2026 第二题明确要求多维交互、HDL 实时合成、至少 4 复音及控制到音频输出延迟不高于 10 ms，并要求单音变调、ADSR 和混音；禁止用预录整段音频代替合成。该题与此建议有直接对应关系。具体扩展分和板卡适配仍应逐条对照[厂商赛题原文](https://www.gowinsemi.com.cn/university/match2026)。

本方案首选 **Tang Nano 20K**。该板在官方适配清单内，板载 64 Mbit SDRAM、HDMI 和 MAX98357A 数字音频功放，适合先用纯 Verilog 打通单音 I2S，再扩展到 4 复音、ADSR 和混音。基础外设为 4—8 Ω 扬声器、电容触摸或按键，以及旋钮或压力传感器；首版不另购音频处理板。若完成基础要求后确有 32 振荡器或复杂效果器的资源需求，再评估 Tang Mega 60K，不在开工时直接提高平台复杂度。硬件信息以 [Tang Nano 20K 官方资料](https://wiki.sipeed.com/hardware/en/tang/tang-nano-20k/nano-20k.html)为准。

### 4.2 可落地的形态

先做固定槽位：4 个触发键、1 个旋钮控制音高或滤波参数、可插入的和弦/音色模块、LED 输出状态。触发与连续控制构成两个独立演奏维度；不能把“识别了两种积木”自动算作两个演奏维度。

首版选择“积木配置声音参数”，例如插入和弦块改变发声组合，旋转旋钮连续变调。先不实现任意音频反馈网络或任意模块拖线，以免引入路由、环路和增益稳定性问题。

```text
触发键、旋钮、槽位模块
        ↓
扫描、去抖、事件时间戳、参数锁存
        ↓
声部分配 → 相位累加器与周期波形表 → ADSR
        ↓
多声部宽位累加 → 增益缩放与限幅 → I2S → DAC
        └────────────────────→ LED / 波形显示
```

FPGA 负责从演奏事件到逐样本生成的完整数据通路。PC 仅用于配置和测试；脱机应能演奏。首版无需摄像头、神经网络或 Linux。

### 4.3 技术深度与资源控制

- 每个声部独立维护相位、频率和包络状态；在内部时钟下可采用多运算单元加时间复用，不为“并行”盲目复制整个链路。
- 初始采用周期波形查表与定点 ADSR；高次谐波丰富的音色要控制高音区混叠，先以正弦等受控波形验证。
- 混音位宽为声部叠加留出余量，缩放后饱和处理，避免整数回绕产生爆音；参数变化加入短平滑过渡。
- 扫描和发声走快路径；槽位配置识别走慢路径，更新在样本边界原子生效。
- 可按 48 kHz 设计。16 声部对应每秒 768,000 次声部更新；假设内部 50 MHz，平均每次约有 65 个周期预算。这只是排程计算，尚需包含包络、访存与混音开销并通过时序验证。
- 第一版 4 复音，随后测量资源再决定扩展到 16 或 32；不要把目标复音数当已实现指标。

### 4.4 验证与展示

首版必须能同时按下 4 键听到独立声部，旋钮变调连续，换积木切换和弦或音色，释放按键出现正确包络。用 GPIO 标记采样到的控制事件，以示波器对比 DAC 模拟输出，测端到端延迟，包含扫描和去抖。

报告：实际采样率、同时活跃声部数、最坏延迟与抖动、资源和时序结果、满复音是否断音或削顶。频谱验证需要采用合适的测试音，不能仅凭复杂音色的谱峰数量宣称独立声部数量。

真正的改进点是**可触摸的多维控制 + 可扩展实时合成数据通路 + 可复现实测**。基本波形和 I2S 本身属于常规实现，不宣称算法首创。

## 5. 原选题一：实体红石升级为可调试数字逻辑平台

建议名称：**基于 FPGA 的实体逻辑积木与可观测时序执行平台**。

技术平台首选 **AMD PYNQ-Z2**：利用其 Zynq-7020、512 MB DDR3、HDMI 输入/输出、2 个 PMOD 及 Arduino / Raspberry Pi 扩展接口连接槽位扫描底板，硬件规格见 [PYNQ 官方板卡页](https://www.pynq.io/boards.html)。PL 端必须负责槽位扫描、Tick 执行、逻辑运算、原子状态提交和波形捕获；PS 端仅承担配置管理和界面显示，不把核心仿真转移到软件。输入与 LED 扩展分别采用 74HC165 和 74HC595，减少直接占用 FPGA 引脚。

该选择是条件式技术方案，不是已确认的参赛资格。采购前必须取得 2026 AMD 企业指南，并确认 PYNQ-Z2 或 XC7Z020 在允许平台范围内；若不允许，则停止 AMD 路线，核对安路 2026 自主命题及其指定板卡后再调整。

### 5.1 先定义连接规则

采用 4×4 槽位原型；需要时再扩至 8×8。每格读取存在位、模块类型、朝向和输入开关状态。模块元数据定义四个边的输入、输出和连线关系，相邻两格只有匹配端口才建立边。

基础模块仅需输入、输出、NOT、AND、OR、XOR、直线/转弯线、中继器。用有方向的接口避免“看上去连着、逻辑上接错”。槽位后端可采用多路选择与移位寄存器串行汇总；具体引脚和扫描电路随板卡确定。I2C ID 方案需解决同地址器件冲突，电阻编码则需 ADC 与容差设计，二者都不是免费能力。

这里连接关系来自**确定的网格邻接规则**。若保留任意跳线，就必须额外做连通扫描或连接端点编码；摄像头看见导线跨越也不能自动判断电气连接。

### 5.2 FPGA 架构与时序语义

```text
槽位扫描 → 稳定配置快照 → 端口检查与网表生成
                                   ↓
                         节点配置 / 连接选择器
                                   ↓
                   组合求值 + 寄存器 / 延迟状态
                                   ↓
                     原子提交 → 实体 LED
                                   └→ 波形 BRAM → HDMI
```

必须明确两种不同语义，选择一种作为首版主模式：

- **教学 Tick 模式：**所有节点读取上一 Tick 状态并同时写下一状态，天然每级门引入一个逻辑 Tick。它是离散时序模型，不能称为普通组合逻辑的零延迟等效实现。
- **电路执行模式：**组合无环网络先分层，在一个逻辑步内求稳，再统一更新触发器。组合环路报错；反馈必须通过寄存器/延迟元件。该模式更接近数字电路教学，但实现成本更高。

首版用 Tick 模式加中继器和显式状态模块，界面说明传播延迟；余力再支持分层求值。不要把双缓冲状态 RAM 的逐节点扫描直接称为所有节点同时计算；应交代具体并行单元数、每步周期数和原子提交时刻。

固定比特流内以配置 RAM 改变节点功能和连接即可，不做现场综合 Verilog 或动态部分重配置。

### 5.3 比原案更值得投入的功能

1. 单步运行、暂停和复位：让用户看到每一级传播。
2. 触发波形记录：例如捕获寄存器输入、时钟与输出，而非只有彩灯变化。
3. 连接错误提示：无驱动、多输出冲突、非法方向、被禁止的组合环。
4. 可重复验证：半加器与全加器遍历全部输入，时序电路对照参考模型。

不把简单 CPU 设为本届必须项。4 位加法器可作为资源允许后的演示，第一版以全加器与延迟链完成闭环。

### 5.4 指标与停止扩展条件

拟定：16 实体槽位稳定工作，64 虚拟节点作为扩展评估；慢速单步到快速自动运行可切换；热插拔后 200 ms 内稳定提交新配置，接触抖动期间不执行半更新网表。64 路、1024 深度、每节点一位状态的基本波形缓存为 8 KiB，实际还需元数据与触发信息。

比较同等语义下 16/64/256 虚拟节点的执行时间、最坏延迟和资源增长；256 仅是压力测试目标，无法满足资源就如实报告上限。若小规模结果显示 MCU 足够，不回避这个事实，强调同步观测和教学交互的工程价值。

若第一周不能可靠识别 4 个槽位与端口关系，立即退回固定插座，停止磁吸自由拼装。视觉识别不与底板扫描同时开工。

## 6. 原选题二：Portal 收敛为视觉驱动的二维光路实验台

建议名称：**基于 FPGA 视觉定位与定点几何引擎的实体光路交互平台**。

本方案首选 **易灵思 VF-Ti60F225**，对应 2026 易灵思第四题“基于 FPGA 的实时图像边缘检测系统”。该平台具备 Ti60F225、DDR3、摄像头输入和 HDMI/LVDS 显示链路，配套方提供摄像头驱动、DDR3 缓存与显示基础工程。Portal 作品应把灰度化、滤波、边缘检测以及基于边缘的目标或姿态提取纳入真实数据通路，再在其上完成传送门、镜面和目标定位；不能只做颜色阈值识别后将边缘检测作为无关演示。板卡与基础工程信息见[易灵思 2026 第四题配套说明](https://www.szovs.com/auspicious-development/interpretation-of-question-of-the)。

采购清单控制为 VF-Ti60F225、配套方明确支持的 CMOS 摄像头和必要下载器；使用现有 HDMI 显示器。购买前先取得基础工程、引脚约束和摄像头准确型号，避免先买不兼容模组。

### 6.1 核心改进

首版使用实体镜面道具、两个有朝向标记的传送门、一个目标和一个舵机门。光线在 HDMI 中计算并显示，命中后驱动实体门。明确“传送”的是数字光路，实体道具不要求真的传输激光。

不加入小球收发、炮塔、移动平台或自动解题。真实光学验证作为后续独立实验，不能把模拟命中称作实测命中。

### 6.2 视觉链路应围绕误差设计

用固定顶视相机、哑光底板、稳定补光、非对称标记。镜面两个定位点用不同颜色或编码解决端点配对，多镜面不能把同色所有像素直接合成一个质心。

首版优先少量独立颜色标记的面积、坐标和包围盒统计，不无条件串入 Sobel、通用连通域与复杂 ISP。出现同色干扰后再引入有限 ROI 或连通域，避免只为增加模块数量而堆算法。

校准镜头畸变与平面映射，将像素坐标映射到统一桌面坐标；标记尽量处于同一高度。斜视情况下不能直接用像素角度作为物理角度。

作为误差预算示例，600 mm 桌面映射到 640 像素时，平均约 0.94 mm/像素。两端标记相距 60 mm、垂直方向差分误差为 2 mm 时，角度误差约 1.9°；镜面反射可使方向误差约翻倍，在 400 mm 距离处造成约 27 mm 横向偏差。这是几何估算，不是实测精度。因此应增大标记间距、限定离散角度并设置合理目标尺寸。

### 6.3 几何引擎

场景表双缓冲，在帧边界提交同一帧的对象快照。FPGA 完成射线与线段求交、最近正向交点选择、反射、传送门局部坐标映射。

建议 8 个对象、最多 8 次传播作为首版规模。使用定点乘加；同一运算单元分时处理候选对象即可，不必 8 套全并行除法器。遇到平行线、近零分母、线段端点与位宽溢出需有明确判定。

反射公式中的法向量必须单位化，或使用带法向量长度平方分母的一般形式。传送门需保留入口切向相对位置，并将入射方向分解到入口坐标系，再按已定义的朝向约定映射到出口；输出起点沿出射方向偏移小量，避免重复命中自身。

8 对象 × 8 次传播仅 64 次候选求交/场景更新，几何部分本身未必是 CPU 瓶颈。FPGA 价值主要应由持续像素流处理、显示并发、固定执行时限和测量结果支撑。

### 6.4 验证与降级

设计目标：640×480、30 fps；稳定识别场景下，移动道具到屏幕更新的 P95 延迟不高于 100 ms。像素流约 9.22 M 有效像素/秒，另需考虑消隐与接口时钟；RGB565 一帧约 600 KiB，双帧约 1.17 MiB，不能假定小板 BRAM 足够。

记录定位误差、角度误差、各关卡命中一致率、失检率、端到端延迟与资源。浮点参考模型用于验证定点几何；实物位置用独立测量基准验证。遮挡时显示失效状态并暂停通关判断，避免用过期坐标误开门。

若第二周尚不能稳定定位两端点，退回网格位置与离散朝向。没有现成摄像头显示工程时，该题在约 7 周内风险高于红石扫描版。

## 7. 原选题三：霉菌检测收敛为可验证的疑似霉斑筛查

建议名称：**面向单一材料的 FPGA 在线疑似霉斑筛查与面积测量系统**。

### 7.1 先回答数据问题

优先选择团队能取得真实样本及可靠标签的一种材料，例如仓储纸板；这是条件示例，不默认你已有样本。任务限定为“指定照明与表面条件下可见疑似霉斑定位”，不要承诺跨材料菌种鉴定、早期不可见霉变或毒素检测。

先收集跨批次样本，并有灰尘、水渍、印刷图案、正常纹理等困难负样本。训练、验证、测试按实物和批次拆分，不能把同一物体的相邻视频帧随机分到不同集合。少量样本只验证可行性，不据此宣称产品准确率。

### 7.2 改进架构

```text
受控补光 + 相机 + 输送位置编码器
                 ↓
FPGA：采集 / 必要的光照校正 / ROI / 位置与帧号
                 ↓
轻量分割或候选区域分类（板卡允许的 CPU / 加速器）
                 ↓
掩膜面积、置信度与拒识 → 工件 ID 和编码器位置队列
                 ↓
FPGA 定位触发 → 标记或分流 → 显示记录
```

先证明前处理能降低带宽或改善识别，再决定硬件化哪些步骤。若相机已输出处理后的 RGB/YUV，不应重复堆完整 Bayer ISP。小型纯 FPGA 板优先考虑传统特征和可承受的轻量模型；异构板也须确认模型算子、存储与工具支持，不能默认任何 NPU 都可直接运行任意 YOLO。

输送链路不能仅用固定“延时若干毫秒”触发分拣。采用编码器累计位置、工件编号与结果队列，处理推理超时、工件间距变化；不确定结果进入复检，不把低置信度强制分类为正常。

### 7.3 怎样证明比常见 AI Demo 更进一步

- 输出真实分割掩膜覆盖率，分母为有效材料区域；不输出未经验证的“危险等级”。
- 对比原图与 FPGA 前处理输入，报告识别性能和传输负载，而非只展示好看的增强画面。
- 同时报告精确率、召回率、困难负样本误报率、分割 IoU、按件吞吐与执行成功率。
- 模型准确性与分拣准确性分开，显示链路帧率与实际推理帧率分开。
- 带速与精度须共同定义：运动速度 v、整链路延迟误差 Δt 会带来位置误差约 vΔt；标称相机帧率不能代替定位误差预算。

首版应先完成固定拍摄台验证，再决定输送机构。只有发现白光下确有稳定识别瓶颈，且有实验数据支持，才增加其他照明波段。仅凭 UV 亮点不构成霉菌鉴定依据。

若第一周拿不到可信正负样本，或无法定义标签来源，本届不把该题列主选。它的主要风险往往是数据与验证，而不是 RTL。

## 8. 综合选择表

| 方案 | 本届赛题适配证据 | 主要 FPGA 工作 | 最大不确定性 | 当前建议 |
|---|---|---|---|---|
| 积木式实时电子乐器 | 高云第二题直接对应；Tang Nano 20K 在适配清单内 | 多声部合成、事件响应、混音、I2S | 团队音频经验、传感器交互稳定性 | 七周完成度优先时首选 |
| 实体逻辑积木 | AMD 平台资格尚待 2026 指南确认 | 网表执行、状态更新、波形捕获 | PYNQ-Z2 参赛资格、扫描底板与时序语义 | 保留创意，确认资格后才采购 |
| Portal 二维平台 | 易灵思 2026 第四题直接提供视频链路和边缘检测任务 | 流式视觉、边缘与姿态提取、定点几何、显示 | 标定、遮挡与延迟 | 赛题匹配度优先时首选 |
| 单材料疑似霉斑筛查 | 具体企业命题待核对 | 前处理、同步采集、位置执行 | 数据与标签、板端推理 | 已有样本和视觉模型时优先 |

若企业题目要求机器人自主导航、倒立摆控制等明确基础任务，以上桌面项目不能仅靠换题目名称满足要求。若已绑定这种题目，应围绕其基础任务重新收敛，而不是硬加外设拼凑。

## 9. 3 人分工与提交倒排

三个成员分别负责：A 数据通路 RTL 与仿真；B 实体输入/传感、板级接口和机械电气；C 参考模型、显示交互、测量工具与文档。C 也承担实现和测试，不仅做 PPT。接口在第一周固定：时钟复位、valid/ready 或事件握手、数据格式、坐标/状态语义与版本号。

| 时间 | 必须完成的交付 | 不通过时的处理 |
|---|---|---|
| 9/14—9/20 | 确定企业赛题与板卡；做最高风险小实验；画出数据与时序接口 | 缩小模块数量或更换输入方式 |
| 9/21—9/27 | 完成可反复运行的最小闭环；按 9/22 截止办理报名 | 删除视觉、机械或复杂模型中的非必要项 |
| 9/28—10/4 | 完成一项核心技术及参考模型对照 | 暂停一切扩展功能 |
| 10/5—10/11 | 首轮全系统联调、资源与时序检查、开始端到端测量 | 根据瓶颈调整分辨率/节点/声部数量 |
| 10/12—10/18 | 加入一项有证据价值的增强功能；形成对比实验 | 性能不达预期则保留已验证版本 |
| 10/19—10/25 | 功能冻结；连续运行、异常输入和重新上电验证 | 只修影响演示与指标的问题 |
| 10/26—11/3 | 完成报告、视频、工程归档和干净环境复现；提前上传检查 | 不再加入新功能 |

提交前必须保留：自己的 RTL/软件修改边界、引用来源与许可核对、综合实现报告、测量原始记录、已知限制、复现步骤。使用开源工程时，沿用基础接口不能包装成原创贡献。

## 10. 定题与采购前检查

已确定的条件为：每个候选方案板卡及基础外设预算 1000—2500 元，团队当前主要具备 Verilog 基础，三个方案最终只选择一个。还必须完成以下检查：

1. Portal：取得 VF-Ti60F225 摄像头到 HDMI 基础工程、引脚约束及配套摄像头准确型号；先验证 640×480 实时边缘输出，再开发 Portal 几何引擎。
2. 红石：取得 2026 AMD 指南并确认 PYNQ-Z2 / XC7Z020 资格；确认后先验证 4 个槽位扫描和逻辑参考模型，不先做摄像头识别。
3. 声音：在 Tang Nano 20K 上先完成单音 I2S 发声，再验证 4 复音、ADSR 和控制到音频输出总延迟不高于 10 ms。
4. 团队补充确认每人每周可投入时间，以及是否有人做过摄像头 HDMI、音频接口、PCB 或机械结构。

任一平台在一周内不能跑通官方或配套基础工程，就缩小功能；不同时更换板卡、摄像头和算法，也不同时购买三套完整系统。
